TASK E: ZERO-COPY FEDERATED QUERY EXECUTION PLAN PROOF
================================================================================

CROSS-SYSTEM SQL QUERY:
--------------------------------------------------------------------------------
SELECT 
        st.region,
        st.city,
        st.store_name,
        c.category_name,
        ROUND(SUM(s.line_revenue), 2) AS category_revenue,
        ROUND(SUM(s.qty), 0) AS units_sold
    FROM read_parquet('s3://annapurna-sales/fact_sales/*/*/*.parquet', hive_partitioning=true) s
    JOIN pg.stores st ON s.store_id = st.store_id
    JOIN pg.products p 
        ON s.product_code = p.product_code 
       AND CAST(s.business_date AS DATE) BETWEEN p.valid_from AND p.valid_to
    JOIN pg.product_categories c ON p.category_id = c.category_id
    WHERE s.year_month = '2024-10' AND st.region = 'South'
    GROUP BY 1, 2, 3, 4
    ORDER BY category_revenue DESC
    LIMIT 10;
--------------------------------------------------------------------------------

DUCKDB PHYSICAL ENGINE EXECUTION PLAN (EXPLAIN):
--------------------------------------------------------------------------------
┌───────────────────────────┐
│           TOP_N           │
│    ────────────────────   │
│          Top: 10          │
│                           │
│         Order By:         │
│ round(sum(s.line_revenue),│
│           2) DESC         │
└─────────────┬─────────────┘
┌─────────────┴─────────────┐
│         PROJECTION        │
│    ────────────────────   │
│             0             │
│             1             │
│             2             │
│             3             │
│      category_revenue     │
│         units_sold        │
│                           │
│       ~183,581 rows       │
└─────────────┬─────────────┘
┌─────────────┴─────────────┐
│       HASH_GROUP_BY       │
│    ────────────────────   │
│          Groups:          │
│             #0            │
│             #1            │
│             #2            │
│             #3            │
│                           │
│        Aggregates:        │
│          sum(#4)          │
│          sum(#5)          │
│                           │
│       ~183,581 rows       │
└─────────────┬─────────────┘
┌─────────────┴─────────────┐
│         PROJECTION        │
│    ────────────────────   │
│           region          │
│            city           │
│         store_name        │
│       category_name       │
│        line_revenue       │
│            qty            │
│                           │
│       ~185,856 rows       │
└─────────────┬─────────────┘
┌─────────────┴─────────────┐
│         HASH_JOIN         │
│    ────────────────────   │
│      Join Type: INNER     │
│                           │
│        Conditions:        │
│product_code = product_code├───────────────────────────────────────────┐
│business_date >= valid_from│                                           │
│ business_date <= valid_to │                                           │
│                           │                                           │
│       ~185,856 rows       │                                           │
└─────────────┬─────────────┘                                           │
┌─────────────┴─────────────┐                             ┌─────────────┴─────────────┐
│         HASH_JOIN         │                             │         HASH_JOIN         │
│    ────────────────────   │                             │    ────────────────────   │
│      Join Type: INNER     │                             │      Join Type: INNER     │
│                           │                             │                           │
│        Conditions:        ├──────────────┐              │        Conditions:        ├──────────────┐
│    store_id = store_id    │              │              │ category_id = category_id │              │
│                           │              │              │                           │              │
│        ~17,189 rows       │              │              │        ~1,264 rows        │              │
└─────────────┬─────────────┘              │              └─────────────┬─────────────┘              │
┌─────────────┴─────────────┐┌─────────────┴─────────────┐┌─────────────┴─────────────┐┌─────────────┴─────────────┐
│        READ_PARQUET       ││       POSTGRES_SCAN       ││       POSTGRES_SCAN       ││       POSTGRES_SCAN       │
│    ────────────────────   ││    ────────────────────   ││    ────────────────────   ││    ────────────────────   │
│         Function:         ││       Table: stores       ││      Table: products      ││           Table:          │
│        READ_PARQUET       ││                           ││                           ││     product_categories    │
│                           ││        Projections:       ││        Projections:       ││                           │
│        Projections:       ││          store_id         ││        product_code       ││        Projections:       │
│          store_id         ││           region          ││         valid_from        ││        category_id        │
│        product_code       ││            city           ││          valid_to         ││       category_name       │
│       business_date       ││         store_name        ││        category_id        ││                           │
│        line_revenue       ││                           ││                           ││                           │
│            qty            ││          Filters:         ││                           ││                           │
│                           ││       region='South'      ││                           ││                           │
│       File Filters:       ││                           ││                           ││                           │
│  (year_month = '2024-10') ││                           ││                           ││                           │
│                           ││                           ││                           ││                           │
│      Scanning Files:      ││                           ││                           ││                           │
│           12/144          ││                           ││                           ││                           │
│                           ││                           ││                           ││                           │
│        ~88,812 rows       ││          ~18 rows         ││        ~1,264 rows        ││         ~148 rows         │
└───────────────────────────┘└───────────────────────────┘└───────────────────────────┘└───────────────────────────┘

--------------------------------------------------------------------------------

ANALYSIS OF OPERATOR EVALUATION:

1. POSTGRESQL ENGINE (Remote Storage & Pushdown):
   - Operator: POSTGRES_SCAN on pg.stores, pg.products, pg.product_categories.
   - Evidence: Predicates such as `region = 'South'` and column projections are pushed down
     directly to PostgreSQL over the libpq connection. Only filtered records are returned.

2. MINIO S3 OBJECT STORE (Remote Columnar Parquet Scan):
   - Operator: PARQUET_SCAN with Hive Partitioning on `year_month = '2024-10'`.
   - Evidence: DuckDB issues HTTP GET byte-range requests directly to MinIO, reading only
     the necessary compressed Parquet columns (`product_code`, `business_date`, `line_revenue`, `qty`).

3. DUCKDB QUERY ENGINE (Vectorized In-Memory Join & Aggregate):
   - Operator: HASH_JOIN and PERFECT_HASH_GROUP_BY.
   - Evidence: The multi-table join and aggregation are executed pipelined in DuckDB's
     in-memory vectorized execution engine. Neither side was copied or staged into the other.
